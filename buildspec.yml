version: 0.2

phases:
  pre_build:
    commands:
      - apt-get update
      - apt-get install -y wget unzip
      - wget https://github.com/awslabs/git-secrets/releases/download/3.1.0/git-secrets -O /tmp/git-secrets
      - chmod +x /tmp/git-secrets
      - sudo mv /tmp/git-secrets /usr/local/bin/git-secrets
      - git secrets --register-aws
      - git secrets --install -f
      - git secrets --scan -r HEAD

  build:
    commands:
      # Your actual build steps go here
      - npm install
      - npm run build

  post_build:
    commands:
      # Sync the build artifacts to the main S3 bucket
      - aws s3 sync build/ s3://${BUCKET_NAME}

artifacts:
  files: '**/*'
  base-directory: build
  discard-paths: yes
